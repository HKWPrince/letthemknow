package io.letthemknow.template;

/** Shape of {@code content_payload}: EMAIL_HTML = {html,text}; LINE_MESSAGES = {messages:[...]} (max 5). */
public enum TemplateType {
    EMAIL_HTML,
    LINE_MESSAGES
}
