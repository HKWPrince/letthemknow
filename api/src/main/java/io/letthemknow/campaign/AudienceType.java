package io.letthemknow.campaign;

/** CSV_LIST: recipients uploaded from CSV. LINE_AUDIENCE_GROUP: one LINE narrowcast to an audience group. */
public enum AudienceType {
    CSV_LIST,
    LINE_AUDIENCE_GROUP
}
